export {
  SUPPORTED_SURFACE_TYPES,
  normalizeSurfaceElement,
  normalizeSurfaceModel,
  validateSurfaceElement,
  validateSurfaceModel,
  isSupportedSurfaceType,
} from "./surfaceModel.mjs";

import {
  SUPPORTED_SURFACE_TYPES,
  normalizeSurfaceElement,
  normalizeSurfaceModel,
  validateSurfaceElement,
  validateSurfaceModel,
  isSupportedSurfaceType,
} from "./surfaceModel.mjs";

export default {
  SUPPORTED_SURFACE_TYPES,
  normalizeSurfaceElement,
  normalizeSurfaceModel,
  validateSurfaceElement,
  validateSurfaceModel,
  isSupportedSurfaceType,
};
